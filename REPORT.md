# 전국 전력수요 1시간 앞 예측

## 1. 주제 선정과 활용 목적

전력수요의 단기 변화를 예측하는 문제는 시간대·요일에 따른 반복성과 기상 조건의 관계를 함께 다룰 수 있다. 본 프로젝트는 2025년 전국 발전단 수요를 대상으로 고전 시계열 모형과 딥러닝을 비교한다. 목적은 LSTM과 Transformer의 활용 가능성을 실험으로 평가하는 것이다. 본 결과는 교육용 회고 실험이며 실시간 전력 운영 시스템의 성능을 검증한 것은 아니다.

연구 질문은 (1) 딥러닝이 단순 지속 예측과 계절성 모형보다 정확한가, (2) 7개 지역 날씨 입력이 LSTM에 도움이 되는가, (3) 평균 오차가 작은 모델이 주말·고수요 시간에도 유리한가이다.

## 2. 데이터와 시간 정렬

한국전력거래소 원본은 2025년 365일의 1~24시 수요를 제공한다. 빈 행 1개를 제거하고 세로 형식으로 변환하면 8,760시간이다. 단위는 MWh이며, 육지·제주를 포함한 발전단 수요의 잠정자료이다. 24시는 다음 날짜 00시로 해석해 시각을 정렬했다. 해당 시간 종료 표기 해석은 실험의 명시적 가정이다.

ASOS는 서울·인천·대전·대구·광주·부산·제주를 포함하며, 기온·강수량·풍속·습도·일조·일사를 사용한다. 지역별 관측값을 개별 입력으로 유지했다. 7개 도시는 전국 모든 기후와 전력 소비를 대표하지 않으며 전력수요 가중치도 적용하지 않았다. 기온 EDA의 7개 지역 평균은 단순 평균이다.

전체 기간은 2025-01-01 01:00:00부터 2026-01-01 00:00:00까지다. 원본 SHA-256과 날짜별 감사 결과는 results/data_audit.json에 기록했다. 통합 데이터는 시간 중복·누락 없이 8,760행이며 목표값은 원본과 일치한다.

## 3. 결측치와 이상치

광주 2025-07-20 12:00의 관측 행 전체가 없다. 실제 존재하는 관측 행 중 풍속 결측은 19개이며 광주 행 누락을 포함하면 풍속 대체 대상은 20개다. 강수·일조·일사에는 빈칸이 많지만 제공받은 파일에 QC 플래그가 없어 무현상과 장비 결측을 완전히 구분할 수 없다.

관측 행 안의 강수·일조·일사 빈칸은 0으로 가정했다. 관측 행 전체 누락과 기온·풍속·습도 결측은 과거 관측값으로 채웠다. 모든 기상 변수에 원래 결측 여부를 나타내는 입력을 추가했다. 미래 값으로 선형 보간하지 않았다. 이는 누출 위험을 줄이지만 0 대체의 물리적 정확성을 보증하지 않으므로 QC 자료를 이용한 민감도 검증이 후속 과제다.

훈련 수요에 IQR 규칙을 적용해 이상치 후보 8개를 표시했다. 수요 급등은 예측해야 할 대상이므로 자동 삭제하지 않았다. 이 문제는 연속값 회귀여서 클래스 불균형 보정은 해당하지 않는다. 대신 고수요 구간의 표본 수와 오차를 따로 보고한다.

## 4. EDA

모델 설계용 EDA는 학습 6,132시간에 한정했다. 전체 연도 그래프는 데이터 범위와 분할을 설명하는 데 사용한다. 학습 구간은 9월 중순까지만 포함하므로 월별 패턴을 1년 전체의 계절성으로 일반화하지 않는다.

- 시간대 평균은 20시에 최대, 4시에 최소다.
- 평일 평균은 67,754.5 MWh, 주말 평균은 59,450.8 MWh다. 공휴일 변수를 별도 반영하지 않아 평일 분류에 공휴일이 섞인다.
- 수요 자기상관은 1시간 0.973, 24시간 0.803, 168시간 0.820다. 최근 값과 일·주 단위 반복을 기준선에 포함할 근거다.
- 7개 지역 평균 기온과 수요의 단순 상관은 0.184다. 시간대·요일 등 교란요인이 있어 인과관계나 기온 효과의 부재로 해석하지 않는다.

EDA 노트북과 figures/01~04 그래프, eda_*.csv에 상세값을 제공한다.

## 5. 예측 조건과 데이터 분할

시점 t를 예측할 때 t-24~t-1의 실제 수요·기상·달력 정보를 사용한다. 모든 모델이 매 시간 직전까지의 관측을 사용할 수 있는 1시간 앞 순차 평가이다. 미래 날씨 실측값과 미래 수요는 입력하지 않는다. 수요와 날씨가 시간 종료 직후 이용 가능하다는 가정을 두며 실제 공표 지연은 다루지 않는다. 24시간 일괄 예측과는 다른 문제다.

- train: 6,132시간, 2025-01-01 01:00:00 ~ 2025-09-13 12:00:00
- validation: 876시간, 2025-09-13 13:00:00 ~ 2025-10-20 00:00:00
- calibration: 438시간, 2025-10-20 01:00:00 ~ 2025-11-07 06:00:00
- test: 1,314시간, 2025-11-07 07:00:00 ~ 2026-01-01 00:00:00

학습 6,132시간 중 최초 24시간은 입력 창 구성에 사용하므로 실제 학습 시퀀스는 6,108개다. 검증·보정·테스트 첫 시점도 직전 구간의 이미 관측된 입력 이력을 사용할 수 있다.

표준화 계수는 학습 자료만으로 계산한다. Epoch와 대표 seed는 검증 RMSE로 선택한다. 별도 보정 구간은 예측구간 폭 계산에만 사용하고, 테스트는 평가에만 사용한다. 테스트 기간은 11~12월 중심이라 여름 최대 수요 예측력을 검증하지 못한다. 과거 버전에서 같은 테스트 기간의 결과를 이미 본 뒤 설계를 수정한 점도 공개한다. 새 연도에서의 독립 검증이 필요하다.

## 6. 모델과 학습 설정

Naive는 직전 1시간, 24시간 전, 168시간 전 값을 그대로 예측한다. SARIMA는 (2,1,2)(1,0,1,24)로 일주기 계절성을 반영한다. statsmodels SARIMAX 클래스를 사용하지만 외생변수는 넣지 않았으므로 결과 명칭은 SARIMA다. 학습 구간에서 추정한 파라미터를 고정하고, 실제 관측이 도착할 때 상태만 갱신한다. 수렴 상태는 sarima_fit.json에 기록했다.

LSTM은 입력·망각·출력 게이트를 통해 과거 정보를 유지하거나 갱신한다. hidden size 32, 1층, 32→16→1 예측 헤드를 사용한다. Transformer는 입력을 32차원으로 투영하고 학습 가능한 위치 임베딩, 4개 attention head, 1개 encoder 층, FFN 64차원과 dropout 0.1을 사용한다. 입력 창 전체가 과거이므로 창 내부의 양방향 attention은 미래 목표값을 보는 것이 아니다.

두 딥러닝 모델은 최근 수요에 예측 변화량을 더하는 잔차 구조다. 식은 y_hat(t)=y(t-1)+delta_hat(t)이다. 표준화한 수요 공간에서 MSE를 최소화한다. 날씨 LSTM과 날씨 없는 LSTM을 비교하지만 입력 수가 달라 파라미터 수까지 동일한 통제 실험은 아니다. 날씨 효과에 대한 근거를 제공하되 엄밀한 인과효과로 주장하지 않는다.

공통 학습 설정: AdamW, learning rate 0.001, weight decay 0.0001, batch 128, 최대 30 epoch, 검증 손실 개선이 6 epoch 없으면 조기 종료, gradient norm 1.0 제한. seed 42·7·123을 반복한다. 대표 모델은 검증 RMSE가 가장 작은 seed를 사용하며 전체 seed 결과도 보고한다. CPU 2개 스레드로 실행했다.

## 7. 평가 결과

MAE는 절대 오차 평균, RMSE는 큰 오차에 더 큰 영향을 받는 제곱평균제곱근 오차다. 두 지표 단위는 MWh다. MAPE는 실제 수요 대비 절대 오차율이며 실제값은 모두 양수다.

| 모델 | MAE | RMSE | MAPE (%) | 구간 포함률 (%) |
|---|---:|---:|---:|---:|
| Naive_1h | 2,186.8 | 2,678.1 | 3.37 | 88.43 |
| Naive_24h | 4,777.2 | 6,953.7 | 7.50 | 87.29 |
| Naive_168h | 2,522.5 | 3,524.0 | 3.85 | 95.21 |
| SARIMA | 621.4 | 881.8 | 0.98 | 90.11 |
| LSTM_weather | 733.9 | 1,010.2 | 1.14 | 92.85 |
| LSTM_calendar | 820.0 | 1,186.7 | 1.26 | 97.95 |
| Transformer_weather | 713.6 | 958.7 | 1.12 | 96.88 |

SARIMA의 RMSE 881.8 MWh가 가장 작다. Transformer와 날씨 LSTM은 직전 1시간 Naive에 비해 RMSE가 각각 64.2%, 62.3% 작지만 SARIMA를 넘지 못했다. 강한 단기 자기상관과 일주기 구조가 고전 모형에 유리했을 가능성이 있다. 이는 결과에 대한 가설이며 모델 차이의 원인을 분리해 증명한 것은 아니다.

날씨 LSTM의 테스트 RMSE는 날씨 없는 LSTM보다 14.9% 작다. 다만 검증 구간에서는 날씨 없는 LSTM이 더 좋았고, 표본 기간과 입력 차원·모델 용량 차이에 따라 순위가 달라질 수 있다.

### 반복 실험

| 모델 | 3개 seed RMSE 평균 | seed 간 표준편차 |
|---|---:|---:|
| LSTM_calendar | 1,258.3 | 79.5 |
| LSTM_weather | 1,055.5 | 41.3 |
| Transformer_weather | 1,044.6 | 91.2 |

표준편차는 3개 seed의 표본 표준편차이며 일반화 성능의 신뢰구간이 아니다. 대표 seed의 순위만 보고 모델의 우월성을 단정하지 않는다.

### 오차 구간 분석

- SARIMA: 평일 RMSE 770.1, 주말 1,106.6, 고수요 1,088.4 MWh.
- LSTM_weather: 평일 RMSE 1,075.5, 주말 830.9, 고수요 1,381.0 MWh.
- Transformer_weather: 평일 RMSE 1,012.7, 주말 813.5, 고수요 1,158.7 MWh.

고수요는 학습 수요 90백분위 이상으로 정의했다. 테스트 고수요 표본은 27시간뿐이라 극단 수요 성능에 대한 결론은 제한적이다. 전체 오차에서는 SARIMA가 가장 좋지만, 주말 구간에서는 두 딥러닝 모델의 RMSE가 더 작았다. 날짜·시간별 개별 오차를 이용한 추가 원인 분석이 가능하다.

## 8. 예측구간

438시간의 독립 보정 구간에서 절대 오차를 정렬한 뒤 ceil((n+1)×0.95)번째 값을 q로 선택한다. 구간은 예측값 ±q다. 별도 보정 자료를 사용한 고정폭 구간이며, 조건부 이분산을 직접 학습하는 확률모형은 아니다. 시계열 의존성과 계절 이동 때문에 명목 95%가 실제 95% 포함률을 보장하지 않는다. 포함률과 폭을 함께 읽어야 하며 구간이 넓다는 이유만으로 좋은 모델이라고 판단하지 않는다.

## 9. 한계와 후속 실험

2025년 1년과 7개 관측소에 한정되어 있고, 테스트도 겨울 중심이다. 공휴일·산업 활동·지역별 수요 가중치와 기상 공표 지연을 반영하지 않았다. 강수·일조·일사 빈칸의 0 대체 가정이 남아 있다. SARIMA는 단변량, 딥러닝은 다변량이라 알고리즘만의 차이를 분리하는 비교는 아니다. 3개 seed 반복과 날씨 제거 실험으로 일부 변동을 확인했지만 폭넓은 하이퍼파라미터 탐색을 수행하지 않았다.

후속 실험은 추가 연도의 rolling 평가, 공휴일 입력, 지역 수요 가중 기온, QC 기반 전처리 민감도 비교, 24시간 일괄 예측과 분위수 학습 순으로 제안한다. 검증 결과로 선택하고 새 테스트는 마지막에 한 번 확인하는 설계를 유지해야 한다.

## 10. 결론

현재 1시간 앞 예측에서는 SARIMA가 전체 오차 기준으로 가장 정확했다. LSTM과 Transformer도 단순 지속 예측보다 오차를 줄였고 주말 구간에서 장점을 보였다. 복잡한 모델 자체가 높은 성능을 보장하지 않으며, 데이터 특성과 예측 조건에 맞춘 기준선 비교가 필요하다.

## 출처

1. 한국전력거래소, 시간별 전국 전력수요량: https://www.data.go.kr/tcs/dss/selectFileDataDetailView.do?publicDataPk=15065266
2. 기상청, ASOS: https://data.kma.go.kr/data/grnd/selectAsosRltmList.do?pgmNo=36
3. Hochreiter & Schmidhuber (1997), Long Short-Term Memory: https://www.bioinf.jku.at/publications/older/2604.pdf
4. Vaswani et al. (2017), Attention Is All You Need: https://arxiv.org/abs/1706.03762
5. statsmodels SARIMAX: https://www.statsmodels.org/stable/generated/statsmodels.tsa.statespace.sarimax.SARIMAX.html

데이터 정의·단위는 공식 제공 페이지를 확인했고, 실험 수치는 제공받은 원본으로 실행한 결과다.
